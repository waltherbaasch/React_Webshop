import React from "react";

const FilterButton = ({ showFilterDropdown, handleCategoryChange }) => {
  return (
    <div>
       <FilterButton
            showFilterDropdown={showFilterDropdown}
            handleCategoryChange={handleCategoryChange}
          />
    </div>
  );
};

export default FilterButton;
